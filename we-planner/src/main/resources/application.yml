server:
  port: 8081
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/we-planner?serverTimezone=UTC&characterEncoding=UTF-8
    username: root
    password: zlshfkdlcm
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
  jpa:
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        show_sql: true
        format_sql: true
        use_sql_comments: true
